- content_for :current_url, new_garden_plant_path
- content_for :title, "Add a Plant"
- content_for :header_content do
  = link_to "Cancel", garden_plants_path(@garden), class: 'ui-btn-left', data: { direction: 'reverse' }
  h1 = content_for(:title)

= render 'form'